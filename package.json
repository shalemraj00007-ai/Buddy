{
  "name": "buddy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "build:web": "npm --prefix web run build",
    "start": "npm --prefix server run start",
    "deploy:start": "npm run build:web && npm run start"
  }
}
